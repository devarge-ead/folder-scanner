# Runtime dependencies (install inside the project virtual environment)
PySide6>=6.7
rapidfuzz>=3.9
python-docx>=1.1
openpyxl>=3.1
xlrd>=2.0.1
PyMuPDF>=1.24
olefile>=0.47

# Build only
pyinstaller>=6.0